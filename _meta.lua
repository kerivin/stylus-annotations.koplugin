local _ = require("gettext")
return {
    name = "stylus_annotations",
    fullname = _("Stylus annotations"),
    description = _([[Annotate PDFs with freehand stylus strokes, saved in a document sidecar.]]),
}
